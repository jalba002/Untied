using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SampleScript : MonoBehaviour
{
    // No hará falta que abras el script per se.

    [Tooltip("Te puedo decir algo por aquí")] public float number = 5f;
    // Start is called before the first frame update


    /// <summary>
    /// Te resumo lo que hace el método Start
    /// </summary>
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
